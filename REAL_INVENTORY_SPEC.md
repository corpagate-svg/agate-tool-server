# リアル在庫・相違チェック・決算棚卸資産機能 仕様まとめ

作成日: 2026-08-30
作成経緯: eBayの在庫同期不具合調査から派生した機能要望

---

## 1. 背景

eBayのSeller Hubで「売れたのに在庫数量が0にならない」現象を実際に調査した。

- 同一注文内でも、商品によって在庫が正しく0になるものと、ならないもの(残り1のまま)が混在する
- ほぼ同時刻に出品された商品同士(出品ID がほぼ連番)でも結果が割れており、プロモーション有無・まとめ買いか単独購入かでは説明がつかない
- → **eBay/eBaymag側の間欠的なバッチ同期の部分的失敗**の可能性が高いという結論に至った(こちら側のツールや実装が原因ではなさそう)

また、現状ツール内の「在庫数(現物)」という列は、名前に反して物理在庫ではなく、**eBayの「All active listings report」CSVのUSサイトのAvailable quantityをそのまま反映しているだけ**(CSV再取込のたびに全行上書き)。つまりeBay側の不具合をそのまま映してしまう。

この状況を受けて、「eBayの自己申告値を信じず、自前で正しい在庫数を持つ」仕組みを追加したい。

**重要**: この「リアル在庫」という1つの数字を、**用途の異なる2つの目的で共用**する方針(別々の仕組みにはしない)。
1. eBayの自己申告値が正しいかをチェックする道具
2. 決算提出用の棚卸資産書類のベースになる数字

---

## 2. 機能1: 「リアル在庫」フィールドの追加

商品ごとに、eBayのCSV再取込では**上書きされない**独自管理の在庫数を持たせる。

- **初期値**: 導入時点の「在庫数(現物)」(=eBayのUS Available quantity)をコピーして開始
- **「リアル在庫確認日」**も併せて持たせる
  - 空欄 = まだeBay値からのコピーのままで、実地棚卸しによる確認がされていない
  - 日付あり = その日に実地確認・修正した
  - リアル在庫を手動編集すると、確認日を自動的に今日の日付にする(明示的に確認日も指定された場合はそちらを優先)

### 注文登録・編集・削除との連動
- 注文タブで注文を登録すると、該当商品ID の「リアル在庫」を数量分(未指定なら1として)自動で減算する
- 注文を編集(数量・商品ID変更)した場合は、差分に応じてリアル在庫を調整する
- 注文を削除した場合は、該当数量をリアル在庫に**加算して戻す**
- 該当商品IDが見つからない場合はエラーにせず、警告(warning)のみを返す。**注文登録自体は失敗させない**
- 上記いずれの自動増減でも、**リアル在庫確認日は変更しない**(確認日はあくまで実地棚卸しの日付であり、注文による自動計算とは区別する)

### 既存の「在庫」タブとの関係
既存の「在庫」タブ(eBayのCSVをそのまま反映する場所)は今まで通り、注文とは連動させない。eBayのCSVを再取込すると全行上書きされる仕様も維持する。

---

## 3. 機能2: 「相違」ページの新規追加

- 商品ごとに、**US在庫数・UK在庫数・AU在庫数・リアル在庫**を横並びで比較する
  - US在庫数 = 既存の「在庫数(現物)」
  - UK在庫数・AU在庫数は**現状どこにも保存されていない**ため、新たにCSV取込時に保存する必要がある(現状は「在庫数不一致」という一致/不一致のフラグだけが計算され、実際の値は保存されていない)
- **出品されている国のみ**を比較対象にする(UK/AU未出品の商品はその列を比較から除外)
- これら全ての値が一致していない商品だけを一覧表示する(一致している商品は表示しない)
- 「リアル在庫確認日」が空欄(=まだeBay値からの初期コピーのままで未確認)の行は、視覚的に区別して表示する(確認済みの行と混同しないように)

---

## 4. 機能3: 決算提出用の棚卸資産書類(リアル在庫を共用)

### 決算日
**4月30日**(5月1日〜新事業年度)

### 必要な機能

**a. 棚卸資産一覧のエクスポート**
- 全商品の「商品ID・商品名・リアル在庫(数量)・仕入価格(単価)・評価額(単価×数量)」を一覧化し、合計評価額も算出する
- 標準的な棚卸資産明細の形式でよい(税理士から特に指定フォーマットはない)
- 基準日(決算日)を指定してエクスポートできるようにする

**b. 「決算日時点でまだ実地確認されていない商品」のチェック機能**
- 決算のためには、全商品を決算日前後に実地棚卸しし直す必要がある
- 「リアル在庫確認日」が決算日から離れている(=今期まだ数え直していない)商品を一覧化し、実地棚卸しの抜け漏れに気づけるようにする

**c. 決算スナップショットの保存(重要)**
- 「リアル在庫」は注文のたびに動き続ける数字なので、決算日を過ぎると値が変わってしまう
- 決算日時点の状態を**確定した記録として保存(エクスポート)**できるようにし、後から見返しても決算時点の数字が変わらないようにする(日付入りのCSV/Excel等での書き出し・保存)

### 運用イメージ
決算日(4/30)前後に全商品を実地棚卸し → 商品ごとに「リアル在庫」を実数に更新(確認日が自動で4/30前後になる)→ bの確認漏れチェック機能で抜けがないか確認 → cのスナップショットをエクスポートして保存し、税理士に提出

---

## 5. 実装上の重要な注意

**このリポジトリ(GitHubのローカルクローン `C:\Users\81805\projects\agate-tool-server`)は、本番VPS上の実際のコードより古い可能性が高い。**

- 別セッションの調査(過去のメモリ記録ベース、未検証)によると、本番には `server/ebay/`(OAuth連携)、`server/inventoryRebuild.js` への分割、「US起点方式」という在庫同期ロジック、`computeTotalStock()`(総在庫個数KPI)など、このリポジトリには存在しない実装がすでにある可能性が高い
- 普段の運用は、VS CodeからVPSに直接接続してコードを編集しており、GitHubへのpushはしていない(デプロイが不要なため)
- **この仕様書はあくまで「やりたいこと」の設計意図であり、実装は本番VPS上の実際の現在のコードに対して行うこと**。このリポジトリ内に別途生成された `server/server.js` への差分コミットは、古いコードに対するものなので使用しないこと
