# agate-tool-server

株式会社アゲイト・物販事業向けの自社ツール(Node.js製、KAGOYA VPS上でセルフホスト)。
売上分析・在庫・注文の3タブを持つダッシュボードを `server/server.js` で提供する。

## eBay API利用制限(重要・厳守)

今後eBay関連の実装(Shopee連携含む)を行う際は、必ず以下を守ること。

- 使用してよいのは **Inventory API(読み取り専用)のみ**
- 取得してよいのは **自分自身の出品情報**(タイトル・価格・在庫数・SKU・商品説明)だけ
- **Fulfillment API(注文情報API)は、今後の明示的な指示がない限り絶対に呼び出さない**
- **購入者の氏名・住所・連絡先など、他のeBayユーザーの個人情報を取得・保存するコードは一切書かない**

### 理由

eBayの「Marketplace Account Deletion」規約対応との整合性のため。eBay側には「Inventory API(自分の出品データ)のみを取得し、他ユーザーの個人情報は一切収集・保存しない」という内容で適用除外(exemption)を申請済みで、OAuth Scopesも `sell.inventory.readonly` のみに設定済み。実装がこの申告内容と異なると規約違反のリスクがあるため、厳密に守ること。

Shopee側は読み取り・書き込み両方を行ってよい(eBayから抽出した商品・出品データをShopeeに出品するため)。この非対称性(eBay=読み取り専用/Shopee=読み書き可)は意図的な方針であり、変更しない。

## リアル在庫機能(2026-08-31 本番反映済み)

eBaymagの国間同期不具合(UKで売れて在庫0になった後、USの古い在庫数がUKへ再同期されて販売可能に戻ってしまう)を当社側で検知・防止するため、eBayの自己申告在庫とは別に「リアル在庫」(当社独自管理の実在庫)を持たせる機能。売上分析・在庫・注文の既存3タブに加えて、「相違」「棚卸」「決算」の3タブを追加した(`server/server.js`のDASHBOARD_PAGE内)。

**在庫管理表.xlsxの列構成**: 既存19列(商品ID〜備考)はそのまま、20列目以降に追加(既存列の番号は一切変更していない)。
`UK在庫数` `AU在庫数` `リアル在庫` `リアル在庫確認日` `棚卸入力数量` `棚卸入力日時` `画像URL` `日本語商品名` `SKU`(全28列)。

**リアル在庫の初期化・更新ルール**: eBay同期・CSV取込のいずれでも、リアル在庫が**未設定の商品のみ**その時点のUS在庫数をコピーして初期化する。**既にリアル在庫が設定済みの商品は、以後のeBay同期・CSV取込では絶対に上書きしない**(`server/ebay/inventorySync.js`・`server/inventoryRebuild.js`の該当ロジック)。増減は注文の登録・編集・削除・キャンセル(`server.js`のhandleAddOrder等)、または棚卸確定(`server/realInventory.js`のconfirmStocktake)からのみ行う。画像URL・SKUはeBay同期のたびに最新値へ更新される(当社独自データではないため)。日本語商品名は当社独自入力のため、いずれの同期でも上書きしない。

**相違タブ**: 「①リアル在庫↔US在庫」「②US在庫↔UK在庫」「③US在庫↔AU在庫」のいずれかが不一致の商品のみ一覧表示(`server/realInventory.js`のcomputeDiscrepancies)。US・UK・AUの「同一商品」判定はタイトル文字列の完全一致のみで行っており(SKU・Item ID対応表は使用不可、過去調査でこの出品者のSKU/価格は使い回しでキーとして使えないと判明済み)、確実に紐付かなかったUK/AU出品は無理に統合せず「未紐付け(要確認)」として相違タブ下部に別枠表示する(未紐付けデータの実体は`ebay/inventorySync.js`が書き出す「UK_AU保管(US未紐付け)」シート)。

**棚卸タブ**: 「棚卸入力数量」への入力は一時保存のみ(この時点ではリアル在庫は変わらない)。「一括確定」で内容をプレビュー確認してからリアル在庫へ反映する2段階方式。異常値(絶対差5以上、または現在値の50%以上の変動)は確定前プレビューで警告表示(`realInventory.js`のisAbnormalDiff)。画像はeBayのGalleryURLをサムネイル表示、クリックでモーダル拡大。日本語商品名はその場で編集・保存可能(1回Claudeが英語タイトルから一括下書き作成、以後は手入力で上書き)。

**検索・ページネーション**:
- 在庫タブ: 500件/ページ、検索(商品名・商品ID)は常に全商品対象、DOM描画も該当ページ分のみ。列構成は変更していない(SKU・画像等は追加していない)。
- 棚卸タブ: 300件/ページ、検索は英語商品名・日本語商品名・商品ID・SKUの4項目を対象(SKUは検索専用で一覧には表示しない)。全商品対象・DOM描画は該当ページ分のみ。ページ移動しても入力済み棚卸数量が消えないよう、保存成功時にクライアント側キャッシュ(invRows)も同時更新している。

**決算タブ**: リアル在庫を棚卸資産の基準値として共用(別データを作らない方針)。未確認商品チェック・xlsxエクスポート・スナップショット保存(`data/snapshots/`への日付入り確定記録、後から変わらない)。

**変更履歴**: `data/在庫変更履歴.xlsx`に追記式で記録(在庫管理表.xlsxは同期のたびに丸ごと作り直されるため、履歴は独立ファイルに保持する設計)。

**本番反映結果(2026-08-31)**: 反映後の初回eBay同期で商品数1,192件(新規22・削除18)、UK在庫取得1,177件、AU在庫取得1,181件、画像URL取得1,192件(全件)、相違29件検出、未紐付け19件。日本語商品名は事前作成した下書き1,188件中1,170件を反映(新商品22件は下書きなしのため未設定)。売上管理表.xlsx(注文データ)は今回の反映で変更していない。

**反映前バックアップ**: `/root/agate-tool-server/pre_realinv_backup_20260831_192058/`(server/一式・data/一式)。ロールバックが必要な場合はこのディレクトリから復元する。

**開発の経緯・詳細な調査記録**: `REAL_INVENTORY_SPEC.md`(初期仕様)、`data-migrations/japanese_names_draft_20260901.json`(日本語名下書き辞書、今後新商品分の追加翻訳にも再利用可)。ローカルのgit履歴(`feature/real-inventory`ブランチ、コミット`b81c22f`〜`b3e51b3`)に実装の全経緯が残っている。
